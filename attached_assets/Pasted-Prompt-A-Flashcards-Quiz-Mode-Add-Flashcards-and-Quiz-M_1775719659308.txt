Prompt A — Flashcards + Quiz Mode

"Add Flashcards and Quiz Mode to Recall. 1) FastAPI POST /api/flashcards that accepts article title, verdict and bullets, calls Claude to generate 8 flashcards as {front: question, back: answer} pairs covering key concepts. Returns JSON array. 2) FastAPI POST /api/quiz that accepts same inputs, generates 5 multiple choice questions each with 4 options and correct answer index, returns JSON array. 3) React FlashcardViewer component — shows cards one at a time with flip animation (front shows question, click to reveal answer), Previous/Next buttons, progress counter '3 of 8'. Add a Flashcards button on every article card. 4) React QuizMode component — shows one question at a time, 4 clickable options, highlights correct/incorrect after selection, shows score at end '4/5 correct', option to retry. Add Quiz button on every article card. 5) Connect flashcards to spaced repetition — when user marks a flashcard as 'Hard', schedule it for review tomorrow."


Prompt B — Infographic Generator

"Add an Infographic Generator to Recall. 1) FastAPI POST /api/infographic that accepts article title, verdict and bullets. Uses Claude to generate a structured infographic layout as JSON: {title, subtitle, sections: [{heading, points: [string]}, ...], key_stat, bottom_line}. 2) React InfographicView component that renders this JSON as a beautiful visual infographic using HTML/CSS — purple header with title, white sections with colored icons, key stat highlighted in a large number box, bottom line in italics. Style it like a professional infographic. 3) Add a Download as PNG button using html2canvas library. 4) Add a Share button that generates a public link to view the infographic. 5) Add Infographic button on every article card and in the summary result view."


Prompt C — Deep Research Mode

"Add Deep Research Mode to Recall. 1) Add a Deep Research toggle on the home page — when enabled, instead of just summarising one URL, Recall performs multi-source research. 2) FastAPI POST /api/deep-research that accepts a research question (not a URL). Uses Claude to break the question into 3-5 sub-questions. For each sub-question, searches the user's saved library for relevant articles using semantic similarity. Also generates 3 Google search queries the user could use to find more sources. Synthesises everything into a comprehensive research report with: executive summary (3 sentences), detailed findings (5-8 paragraphs with citations to saved articles), knowledge gaps (what's missing from their library), recommended next reads (3 suggested topics). Returns as structured JSON. 3) React DeepResearchView that renders the report beautifully with section headers, inline citations linking to saved articles, and a knowledge gaps warning box. 4) Add Save Report button that saves the entire report as a new item in their library. 5) Add Deep Research button in the nav bar."


Prompt D — Slide Deck Generator

"Add a Slide Deck Generator to Recall's Creators page. 1) FastAPI POST /api/slides that accepts article title, verdict, bullets and optional custom instructions. Uses Claude to generate a 8-10 slide presentation as JSON array: each slide has {slide_number, layout (title/content/two_column/quote/stats), title, content, speaker_notes}. 2) React SlidePreview component that renders each slide as a visual card matching its layout — title slides have large centered text, content slides have bullet points, quote slides show pull quotes in large italic text. Navigation arrows to browse slides. 3) Download as PDF button using jsPDF library — generates an actual downloadable PDF presentation. 4) Download as PPTX using PptxGenJS library — generates a proper PowerPoint file. 5) Add Generate Slides button on every article card and in Creators page. Show slide count badge on the preview."


Prompt E — Export Everything + Data Ownership

"Add full data export to Recall's Settings page. 1) FastAPI GET /api/export/json that returns all of the user's saved articles, highlights, collections as a single JSON file download. Include all fields: id, url, title, verdict, bullets, article_text, recall_score, credibility_score, collection, highlights, created_at. 2) FastAPI GET /api/export/markdown that converts every saved article into a Markdown file and returns them as a ZIP download. Each file named by article title, containing: # Title, source URL, date saved, verdict, bullets as a checklist, highlights. 3) FastAPI GET /api/export/csv that exports titles, URLs, verdicts, recall scores and dates as a CSV. 4) React ExportSection in Settings showing three download buttons: Export as JSON, Export as Markdown (ZIP), Export as CSV. Each shows file size estimate and last export date. 5) Add an Import from JSON button that accepts a previously exported JSON file and re-imports all articles into the user's library — enabling migration from old account or backup restore."